# 99 -- SCOPE: `$29540C`, THE THING THAT STOPS THE BOSS

status: **SCOPED AND NOT TAKEN.** Nothing was written. Nothing changed.

2026-08-06. wave: 98's second item. role: IMPLEMENTER.
target: `ddpdojblk` VERSION-B. `[M]` = measured by me, this session.

> the owner: *"Can we actually make sure we fix the thing that stops the boss
> too? Something unported I guess?"*

**Their guess is right and the address is right.** `[M]` the browser run stops at
lf8,518 with `UNPORTED $29540C`, which is **F script 3's INIT**. The brief that
brought this to me said: size it first, and *"if this is bigger than a fold-in,
say so in the worklog and stop rather than half-landing it."*

**IT IS BIGGER THAN A FOLD-IN. `[M]` IT IS 21 ENTRY POINTS AND ~701 BOSS-LOCAL
INSTRUCTIONS -- W95'S WHOLE WAVE WAS 690.**

---

## 1. WHY THE ENTRY POINT'S OWN SIZE TELLS YOU NOTHING

W96 §1.2's lesson exactly: `$294FA0` was ONE instruction and MAIN 0 behind it
was 252. `[M]` here:

```
[M] 29540C  102e003f  move.b $3F(A6),D0     <- F 3's INIT
[M] 295410  d02e007f  add.b  $7F(A6),D0
[M] ...
[M] 29542C  4eb9002598d0  jsr $2598D0       <- MAIN.start 3, UNCONDITIONAL
[M] 295432  102e003f  move.b $3F(A6),D0     <- and it FALLS THROUGH into the STEP
```

`[M]` F 3 itself is **139 instructions** (74 + 65). **The `jsr $2598D0` with
`moveq #$3` at `$29542A` is in the INIT and is not gated by anything**, so the
very first frame F 3 runs, MAIN script 3 is armed and the NEXT frame the port
throws on `$2934A2` instead. There is no smaller subset.

## 2. THE TRANSITIVE CLOSURE, RESOLVED THROUGH THE REAL TABLES

`[M]` the A0/A1/A3/A4 table bases read out of `stage1-sweep`'s own RAM at
lf10,000 (`$812984` = `$293104`, `$812BD4` = `$295856`, `$812A70` = `$29370A`,
`$812D38` = `$294F68`), the ported set DERIVED from the port's own 57
`registerScript` calls rather than typed:

| what F 3 starts | entry points | state |
|---|---|---|
| **MAIN 3** `$2934A2`/`$2934AC` | 2 | **UNPORTED** |
| MAIN 5 `$293578`/`$29359E` | 2 | ok (W96) |
| **D 16** `$2948B6`/`$2948C4` | 2 | **UNPORTED** |
| **D 17** `$29492E`/`$29493C` | 2 | **UNPORTED** |
| **D 18** `$2949A6`/`$2949BA` | 2 | **UNPORTED** |
| **D 19** `$294A30`/`$294A44` | 2 | **UNPORTED** |
| **E 8** `$296362`/`$2963A2` | 2 | **UNPORTED** |
| F 6 `$295684`/`$2956F6` | 2 | ok (W95) |

and those in turn:

| started by | what it starts | state |
|---|---|---|
| MAIN 3 | **MAIN 4** `$2934F8`/`$293506` | **UNPORTED** |
| MAIN 3 | MAIN 2, MAIN 7 | ok |
| D 16 / D 17 | **D 10** `$2944DE`/`$2944E6`, **D 11** `$294512`/`$29451A` | **UNPORTED** |
| D 18 / D 19 | D 2, D 3 | ok (W96) |
| E 8 | nothing | -- |

**`[M]` 18 unported entry points in the closure, plus F 3's own two, plus one
missing scheduler accessor = 21.**

### 2.1 The accessor, and it is the only cheap part

`[M]` `$2599B4` -- **IS A3 SCRIPT D0 RUNNING?** 16 instructions, the exact A3
analogue of `$259A4A` (which IS ported as `a1Running259A4A`): ten slots at
`$812A74` stride `$20`, `andi.w #$FF`, `cmp.w D0`, C set on found. **This is the
fifth scheduler accessor W62 did not ship**, W96 §1.1 named it, and both F 2 and
F 3 need it. It is a fold-in on its own and it is DEAD CODE on its own, so it is
not shipped alone.

### 2.2 The instruction census

`[M]` static closure over all 21 roots (`.scratch/w96/walk.py`; `jsr (An)` is
invisible, so this is a **LOWER BOUND**):

```
[M] CLOSURE  41 routines  1,011 instructions
```

of which the boss-local script bodies are:

```
[M] F 3     $29540C  74 + $295432  65 = 139
[M] MAIN 3  $2934A2  68 + $2934AC  66 = 134
[M] MAIN 4  $2934F8  71 + $293506  68 = 139
[M] E 8     $296362  67 + $2963A2  54 = 121
[M] D 18/19 $2949A6 + $294A30 and their steps  = 66
[M] D 16/17 $2948B6 + $29492E and their steps  = 58
[M] D 10/11 $2944DE + $294512 and their steps  = 28
[M] $2599B4                                    =  16
[M]                                       TOTAL  701
```

**Against W95's entire wave at 690 boss-local instructions and W96's at 1,526.**
Every helper F 3 itself calls is already ported except `$2599B4`: `[M]`
`$2598C8`, `$2598D0`, `$259962`, `$259A18`, `$259A4A`, `$25980C` and `$242FDE`
all exist in `src/scheduler.js` and `src/rng.js`.

## 3. **AND IT PROBABLY DOES NOT RESTORE `STAGE 1 ENDS` ON ITS OWN**

`97-OWNER` records that the red is *"solely because the run stops at `$29540C`
before reaching the ending the gate walks to"*, and offers porting it as the way
out. That is true of the throw the gate reports **today**. It is not the whole
path.

`[M]` **F SCRIPT 2 IS UNPORTED AND IS THE OTHER HALF**, exactly as W96 §7 said:

```
[M] the ONLY site in the whole boss range that starts F 2 is $29584C
[M] (`moveq #$2,D0 / jsr $25980C`), inside F 6's own tail
[M] F 2 starts: MAIN 8, D 8, D 9, D 12, D 13, D 14, D 15  -- 14 more
[M] unported entry points, and it needs $2599B4 too
```

W96 §5.1's blocking census on `stage1-sweep` is the same statement from the
ladder: **10 rungs blocked on `$29540C` and 9 on F 2's `$2952D8`/`$295304`.**
The gate walks to lf18,675 / lf19,150 / lf19,223; F 6 runs long before that and
`$29584C` is in it.

**So `$29540C` alone would move the throw, not remove it**, and `STAGE 1 ENDS`
would stay red with a different reason on the line. Reporting that is the point
of the brief's *"if porting it does NOT restore `STAGE 1 ENDS`, that is a
finding"*.

## 4. WHAT I DID NOT DO, AND WHY IT IS THE RIGHT CALL

* **Nothing was written.** No file under `src/` was touched by this item.
* **The gate was not forced green.** No clamp, no stub, no special case. The red
  stays where `97-OWNER` put it, carried on purpose with its reason in two
  documents rather than one.
* **`$2599B4` was not shipped alone.** It is 16 instructions and it is dead
  until F 2 or F 3 exists.

The owner is away and four further items they reported by name were queued
behind this one. A wave the size of W95, landed half-way with the tree left
mid-port and the scheduler throwing on a different address, is worse for them
than an honest scope. **This is the "queued, not mandated" of `97-OWNER` §"THE
WAY OUT" turning out to have a price, measured.**

## 5. THE SHAPE OF THE WAVE, FOR WHOEVER TAKES IT

**ONE wave, and it is W96 §7's own two-item list. Do F 2 and F 3 together.**
They share `$2599B4`, they share `$2598D0`'s MAIN restarts, they are the last
two named throws on the whole ladder, and `[M]` between them they close **35
entry points** and every one of the 29 blocked segments of `stage1-sweep`.

Order inside it:

1. `$2599B4` -- 16 instructions, and both halves need it.
2. **MAIN 3 and MAIN 4** (`$2934A2`, `$2934F8`) -- 273 instructions, and F 3's
   INIT arms MAIN 3 unconditionally, so nothing else runs until they exist.
3. **D 10/11/16/17/18/19** -- 152 instructions in six pairs, each a 3-4
   instruction INIT and a ~27 instruction STEP. `[M]` W96 §1.4's `+-2` sites
   `$294918` and `$294A12` are D 18's and D 19's, so the **even speed levels
   W96 exported are what these index** -- that export was made for this wave.
4. **E 8** (`$296362`) -- 121 instructions, starts nothing.
5. **F 3** (`$29540C`) -- 139 instructions, and it falls through into its STEP.
6. **F 2** (`$2952D8`) and its seven D scripts + MAIN 8.

**READ THE FALL-THROUGHS RATHER THAN THE LABELS.** `[M]` F 3's INIT falls
through into `$295432`; W96 §2.1 counted six exceptions to the house rule
already and §3 is what getting one wrong cost -- a defect no unit test and no
traced column could see.

**AND `STAGE 1 ENDS` IS ONLY OWED BACK WHEN BOTH LAND.** §3.
